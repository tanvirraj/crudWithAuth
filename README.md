basic boilerplate template to start `node-typescript-express` api

install packages
`pnpm i`

Run your server by running
`pnpm dev`
"dev": "tsx watch src/app.ts",
"build": "tsc -p tsconfig.json",
"start": "node dist/src/app.js",
